<%= form_for(@baseline_datum) do |f| %>
  <% if @baseline_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@baseline_datum.errors.count, "error") %> prohibited this baseline_datum from being saved:</h2>

      <ul>
      <% @baseline_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<% unless @baseline_datum.persisted? %>
	  <%=f.hidden_field :service_id,:value=>params[:service_id]%>
	<%end%>
  <div class="field">
    <%= f.label :monthly_collection %><br>
    <%= f.text_field :monthly_collection %>
  </div>
  <div class="field">
    <%= f.label :monthly_charge %><br>
    <%= f.text_field :monthly_charge %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
